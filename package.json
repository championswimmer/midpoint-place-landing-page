{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-seo.js && node scripts/generate-sitemap.js",
    "postbuild": "cp -r js dist/js && cp -r images dist/images",
    "generate-seo": "node scripts/generate-seo.js",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2",
    "vite": "^5.4.2"
  }
}
